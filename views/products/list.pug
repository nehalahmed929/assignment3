extends ../layout

block content
    .row
        .col-md-8
            h1 list of products
        .col-md-4
            a(href="products/add" class="btn btn-success float-right") Add New Product
    
    table.table
        thead
            th name
            th price
            th actions
        tbody
            each product in products
                tr
                    td=product.name
                    td=product.price
                    td  
                        a(href="/products/delete/"+product._id, class="btn btn-danger") Delete
                        a(href="/products/edit/"+product._id, class="btn btn-warning") Edit
                        a(href="/products/cart/add/"+product._id, class="btn btn-info") Add to Cart   